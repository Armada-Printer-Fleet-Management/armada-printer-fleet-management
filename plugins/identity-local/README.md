# `plugins/identity-local/` — user and role lookup

Implements the identity port, backed by the local database or by an external directory through
a configured field mapping.

An existing user table elsewhere is this adapter with a different mapping in `config/`, not a
fork of this code.
