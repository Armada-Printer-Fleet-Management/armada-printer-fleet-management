info:
  name: Health
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/health_check"
  params:
    - name: full
      value: "true"
      type: query
      description: Shows detailed information
      disabled: true
    - name: check_dependencies
      value: "true"
      type: query
      description: Runs depedency health check functions
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
